export class Theme {
    constructor(pageColor, cellColor, infoTextColor, cellTextColor, borderColor) {
        this.pageBGColor = pageColor;
        this.cellBGColor = cellColor;
        this.infoTextColor = infoTextColor;
        this.cellTextColor = cellTextColor;
        this.boardBorderColor = borderColor;
    }
}